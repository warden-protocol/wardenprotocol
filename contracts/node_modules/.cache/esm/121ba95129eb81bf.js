let generateEndpointAccount;_29e‍.w("@evmos/provider",[["generateEndpointAccount",["generateEndpointAccount"],function(v){generateEndpointAccount=v}]]);

(async function main() {
  const address = 'qredo1d652c9nngq5cneak2whyaqa4g9ehr8psyl0t7j'

  const nodeUrl = 'http://0.0.0.0:26657'
  const queryEndpoint = `${nodeUrl}${generateEndpointAccount(address)}`

  const restOptions = {
    method: 'GET',
    headers: { 'Content-Type': 'application/json' },
  }

  const rawResult = await fetch(
    queryEndpoint,
    restOptions,
  )

  const result = await rawResult.json()
  _29e‍.g.console.log(result)
})()