export const expectSuccess = (response) => {
    expect(response.tx_response.code).toBe(0);
};
export const delay = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
export const hexToBytes = (hex) => Buffer.from(hex.replace('0x', ''), 'hex');
export const base64ToBytes = (base64) => Buffer.from(base64, 'base64');
//# sourceMappingURL=common.js.map